/****************************************************************************
 * gameObjects.js
 *
 * The GameObjects class is used in Simon Says to contain all the data
 * objects that persist throughout the game.
 *
 * This object is passed between all of the game states as one of their
 * dependency paramters.
 *
 * The objects are instantiated at the start of the game and from then are
 * enable/disabled as needed by each state.
 ***************************************************************************/

( function ( simonSays, undefined )
{
} ( window.simonSays = window.simonSays || {} ));
